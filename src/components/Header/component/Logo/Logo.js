import { LogoWrapper, LogoAccent } from './Logo.styles';

export const Logo = () => {
  return (
    <LogoWrapper>
      <p>
        pe<LogoAccent>t</LogoAccent>ly
      </p>
    </LogoWrapper>
  );
};
